# Demo Video Script

Target length: under 3 minutes.

1. Show the problem: production ML failures hide in lineage and metadata.
2. Start with DataHub seeded demo lineage.
3. Run `mlguard scan` against `credit_risk_v3`.
4. Open `report.html` and show the lineage graph plus findings.
5. Open `sample_mcp_dryrun.json` to show safe write-back payloads.
6. Explain that apply mode requires `--confirm`.
7. Close with why this helps humans and agents inherit risk knowledge through DataHub.
